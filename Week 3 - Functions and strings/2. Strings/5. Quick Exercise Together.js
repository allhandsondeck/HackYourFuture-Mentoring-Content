// Build this together
const shoutGreeting = (name) => {
  const greeting = `Hello, ${name}!`;
  return greeting.toUpperCase();
};

console.log(shoutGreeting("Alice"));

// const shoutGreeting = (name) => `Hello, ${name}!`.toUpperCase();
